%h1= t('titles.review.revise')

%h2= t('titles.review.translated_by', user: @game.translations.last.user.name)

= form_for @game, url: review_update_path, html:{ multipart: true } do |f|
  = render partial: 'form', locals:{ f:f }